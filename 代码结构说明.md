# 代码结构说明（给 AI 或开发者参考）

> 这是「慕言のCOS摄影」个人网站的代码地图。网站是**纯静态单页**：`index.html` 一个文件包含全部 HTML / CSS / JS，不依赖任何服务器，双击即可打开，上传到 GitHub Pages / Netlify 即可上线。

---

## 一、文件结构

```
我的个人网站-蕾姆风/
├── index.html            ← 网站全部代码（HTML + CSS + JS 内联）
├── 代码结构说明.md        ← 本文档
├── 使用说明.md            ← 给站主看的上传/修改教程
└── images/               ← 照片文件夹
    ├── 正片/             ← 正片 46 张（外景 28 / 棚子 18）
    ├── 场照/             ← 场照 59 张（亮灰 33 / 暗调 26）
    ├── 特效/             ← 特效 23 张
    ├── thumbs/          ← 电脑端 WebP 缩略图
    ├── thumbs-mobile/   ← 手机端 WebP 缩略图
    ├── qq.png            ← 页脚 QQ 大号二维码
    ├── qq-small.png      ← 页脚 QQ 小号二维码
    └── wechat.png        ← 页脚微信二维码
```

---

## 二、页面区块（从上到下）

| 区块 | 标记 | 说明 |
|------|------|------|
| 首页横幅 | `<header class="hero">` | 大标题「用镜头，定格最美的你」+ 副标题 + 3个按钮 |
| 标语横幅 | `.slogan-banner` | 「慕言——提供优质服务，上乘质量，适中价格！」 |
| 作品例图 | `<section id="works">` | 正片/场照/特效 **选项卡**切换；首批固定 3 张首图，之后每批随机 1横+2竖且不与上一批重复 |
| 价格 | `<section id="price">` | 正片/场照/特效 **价格选项卡**；定金+尾款+合计；特效加购 |
| 注意事项 | `<section id="notes">` | 正片须知/场照须知 **选项卡**；FAQ 手风琴；服务福利 |
| 心里话 | `.quote` | 温馨横幅 |
| 页脚 | `<footer id="contact">` | QQ 大号、QQ 小号、微信号、三张二维码与独立复制按钮 |

---

## 三、CSS 关键类名

| 类名 | 作用 |
|------|------|
| `.hero` / `.hero-inner` | 首页横幅 |
| `.slogan-banner` / `.slogan-main` | 标语横幅 |
| `.works-tabs` / `.work-tab` | 作品选项卡（正片/场照/特效） |
| `.cat-tabs` / `.cat-tab` | 风格选项卡（全部/外景/棚子、全部/亮灰/暗调） |
| `.photo-block` / `.wide` / `.n1` / `.n2` | 1横+2竖排版 |
| `.grid` | 照片容器（JS 动态填充） |
| `.card` | 单张照片卡片 |
| `.refresh-row` / `.refresh-btn` / `.gallery-hint` | 换一批按钮区 |
| `.price-tabs` / `.price-tab` | 价格选项卡 |
| `.price-card` / `.tier` / `.tier.total` | 价格卡片/行/合计行 |
| `.fx-card` | 特效加购卡 |
| `.note-tabs` / `.note-tab` / `.note-panel` | 须知选项卡 |
| `.faq` / `.faq-group` / `.faq-body` | FAQ 手风琴 |
| `.lb` / `.lb-img` / `.lb-prev` / `.lb-next` | 灯箱（点击放大） |
| 媒体查询 | 手机端适配（2列瀑布、按钮加大等） |

---

## 四、JS 关键对象与函数

### `GALLERIES`（照片数据源）
```js
GALLERIES = [
  {
    key: 'zhengpian',          // 板块标识（与HTML里 id 对应）
    cats: ['外景', '棚子'],    // 风格分类
    fixed: {                   // 首次进入/刷新页面时固定展示的三张首图
      all: ['images/正片/外景/首图.jpg', ...],
      外景: ['images/正片/外景/首图.jpg', ...],
      棚子: ['images/正片/棚子/首图.jpg', ...]
    },
    images: [                  // 照片列表
      { s: 'images/正片/外景/01.jpg', c: '文件名', w: 2400, h: 1600, cats: ['外景'] },
      ...
    ]
  },
  ...
]
```

### 函数一览
| 函数 | 作用 |
|------|------|
| `fixedBatchOf(g)` | 读取当前板块/风格的三张固定首图 |
| `planBatch(g, state)` | 随机规划下一批 3 张：1横+2竖；批内不重复，也不与上一批重复 |
| `renderGallery(key)` | 首次渲染固定首图；点击换一批后渲染随机批次并预载下一批缩略图 |
| `refreshGallery(key)` | 「换一批」按钮入口（强制换新一批） |
| `switchWorks(w)` | 作品选项卡切换（正片/场照/特效），切换时重新适配高度 |
| `show(i)` / `closeLb()` | 灯箱：点击放大 / 关闭；支持左右箭头、ESC |
| `shuffle(a)` | 数组洗牌 |

### 选项卡绑定（都是 `click` 监听）
- `.work-tab` → 作品切换
- `.cat-tab` → 风格切换（首次进入该风格时显示对应三张首图）
- `.price-tab` → 价格切换
- `.note-tab` → 正片须知/场照须知切换

---

## 五、常见修改场景速查

| 想改什么 | 去哪里改 |
|----------|---------|
| 改价格数字 | 搜索 `定金` / `尾款` / 具体数字（如 `329`） |
| 改文字/标语 | 搜索中文关键词，如「慕言——」「保底」「迟到」 |
| 换照片 | 在五个作品目录中增删原图，再使用版本目录中的 `维护工具/rebuild_gallery.py` 重建数据和两套缩略图 |
| 设置首图 | 每个作品目录保留且只保留 3 个以 `首图` 开头的文件；程序会按 1 横 + 2 竖排序 |
| 加新风格分类 | `GALLERIES` 数据的 `cats` 字段 + HTML 里的 `.cat-tab` 按钮 |
| 调整每批张数/排版 | `planBatch()` 和 `.photo-block` CSS |
| 改配色（蕾姆蓝） | 搜索 `:root` 变量（--blue 等） |
| 改手机端样式 | 搜索 `@media(max-width:560px)` 等媒体查询 |

---

## 六、注意事项

1. **不要删除** `GALLERIES` 里照片的 `w` / `h` 字段——随机成组和防抖动高度计算都依赖它
2. 每个作品目录必须有 3 张以 `首图` 开头的图片，并满足 1 张横图、2 张竖图
3. 原图、`images/thumbs/`、`images/thumbs-mobile/` 与 `GALLERIES` 必须同步更新
4. 修改后应通过本地 HTTP 服务预览，不建议只双击 HTML
5. 若改了 `cats` 分类，需同步更新 HTML 里的 `.cat-tab` 按钮
