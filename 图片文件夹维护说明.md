# 图片文件夹维护说明

为方便后期维护，作品图片已经按板块和风格分别存放：

```text
images/
├── 正片/
│   ├── 外景/        正片外景（28 张）
│   └── 棚子/        正片棚拍（18 张）
├── 场照/
│   ├── 亮灰/        亮灰场照（33 张）
│   └── 暗调/        暗调场照（26 张）
└── 特效/            特效作品（23 张）
```

`images/thumbs/` 和 `images/thumbs-mobile/` 内使用相同的分类结构，分别存放普通缩略图和 QQ 手机端缩略图。

每个作品目录必须保留 3 张以 `首图` 开头的图片。程序会自动把其中的横图放在第一格、两张竖图放在第二和第三格；首次打开网页、刷新页面或首次进入风格时固定展示这三张。

增删照片后使用版本目录中的 `维护工具/rebuild_gallery.py`，它会按实际文件重新生成两套缩略图、图片尺寸、分类与固定首图清单，同时清理已删除照片遗留的缩略图。不要手工只改其中一处。

根目录 `_redirects` 已保留旧平铺路径、中文分类路径和中文加拼音分类路径的兼容，并为常见入口提供首页重定向。上传 Netlify 时请连同 `_redirects` 一起上传。
