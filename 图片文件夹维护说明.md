# 图片文件夹维护说明

为方便后期维护，作品图片已经按板块和风格分别存放：

```text
images/
├── 正片/
│   ├── 外景/        正片外景（01.jpg～32.jpg）
│   └── 棚子/        正片棚拍（33.jpg～51.jpg）
├── 场照/
│   ├── 亮灰/        亮灰场照（01.jpg～30.jpg）
│   └── 暗调/        暗调场照（31.jpg～59.jpg）
└── 特效/            特效作品（01.jpg～22.jpg）
```

`images/thumbs/` 和 `images/thumbs-mobile/` 内使用相同的分类结构，分别存放普通缩略图和 QQ 手机端缩略图。

正片 `11.jpg` 在网页数据中同时属于“外景”和“棚子”，实体文件统一放在“外景”文件夹，两个筛选选项仍然都能显示它。

删除照片时，除了删除原图，也需要同步删除两套缩略图，并从 `index.html` 的 `GALLERIES` 中删除对应照片对象；请保留其他照片的文件名及所有 `w` / `h` 字段。

根目录 `_redirects` 已保留旧平铺路径、中文分类路径和中文加拼音分类路径的兼容，并为常见入口提供首页重定向。上传 Netlify 时请连同 `_redirects` 一起上传。
