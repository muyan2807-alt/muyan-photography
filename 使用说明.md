# ❄ 慕言のCOS摄影 · 个人网站使用说明

## 📦 文件结构

```
我的个人网站-蕾姆风/
├── index.html              # 网站主页（改文字、价格都在这）
├── images/                 # 图片文件夹
│   ├── zhengpian/          # ⭐ 正片板块（51 张）
│   ├── changzhao/          # ⭐ 场照板块（59 张）
│   ├── texiao/             # ⭐ 特效板块（22 张）
│   ├── qq.png              # QQ 大号二维码
│   ├── qq-small.png        # QQ 小号二维码
│   └── wechat.png          # 微信二维码
└── 使用说明.md              # 本文件
```

---

## 🖼️ 一、图片墙功能说明

- 每个板块**每次展示 1 张横图 + 2 张竖图**（杂志式排版，照片完整无裁切）
- **风格选项卡**：正片可切换「全部 / 外景 / 棚子」，场照可切换「全部 / 亮灰 / 暗调」，访客自己选择看什么风格
- 点「🔄 换一批」：随机换一批，**尽量不与上一批重复**（看完一轮才会重复）
- 点图片可**放大查看**（灯箱），左右箭头切换、ESC 关闭；放大时**不显示照片名字**
- 卡片上**不显示任何照片名字**（只展示照片本身）
- 所有图片已优化：最长边最高 2400px、JPEG 质量 82

---

## 📸 二、怎么换照片（推荐：发给我）

**把新照片直接发给我**，我会帮你压缩、分类、命名、放进文件夹、更新数据、重新打包。
你只需要把新文件**重新上传到 GitHub**（同名自动覆盖）就行。

**自己换也完全可以：**
1. 新照片放进对应文件夹（正片→`images/zhengpian/`，场照→`images/changzhao/`，特效→`images/texiao/`）
2. 文件名改成编号，如 `05.jpg`、`06.jpg`（接着现有编号）
3. **注意**：网页里的图片列表写在 `index.html` 的 `GALLERIES` 数据里，需要同步修改：
   - 搜索 `GALLERIES`，找到对应板块的 `images` 数组
   - 加一行 `{s:'images/正片板块/05.jpg', c:'', w:宽, h:高, cats:['风格']}`
   - `cats` 里填风格：正片填 `外景` 或 `棚子`，场照填 `亮灰` 或 `暗调`，特效留空 `[]`
   - `w`/`h` 是图片宽高（用于自动判断横图竖图），填错会导致排版乱
   - **建议直接发给我处理，最省事**

---

## ✏️ 三、自己改文字的方法（可选）

用**记事本**打开 `index.html`，按 `Ctrl+F` 搜索替换：

| 想改的内容 | 搜索关键词 |
|-----------|-----------|
| 网站名字 | `慕言のCOS摄影` |
| 宣传标语 | `提供优质服务，上乘质量，适中价格` |
| 首页大标题 | `用镜头，定格最美的你` |
| 价格数字 | `定金` / `尾款` / `A 套餐` |
| QQ 号 | `2745867337` |
| 正片须知内容 | 在 `note-zp` 面板里改 |
| 场照须知内容 | 在 `note-cz` 面板里改 |

改完保存，重新上传即可。

---

## 🌐 四、更新到 GitHub（别人看到新版）

1. 打开你的仓库：`https://github.com/muyan2807-alt/-`
2. 点 **Add file** → **Upload files**
3. 把新版 `index.html` 和 `images` 文件夹**整个拖进去**（同名自动覆盖）
4. 点 **Commit changes**
5. 等 1~2 分钟，访问 `https://muyan2807-alt.github.io/-/` 就是新版了！

> ⚠️ 图片较多（约 82MB），网页上传可能要等一会儿，耐心等上传完成再点 Commit。
> 如果网页上传太慢/超时，推荐用 **GitHub Desktop** 软件上传，更快更稳。

---

## ⚠️ 五、常见问题

| 问题 | 解决 |
|------|------|
| 照片显示不出来 | 确认 `images` 文件夹被上传了，且文件名对应 |
| 朋友看到旧版 | 让朋友按 `Ctrl+F5` 强制刷新 |
| 换一批后重复 | 每批 3 张，看完一轮才会重复，属正常现象 |
| 想加更多照片 | 发给我，我帮你压缩+分类+更新 |
| 想改风格选项 | 告诉我，我帮你加/改选项卡 |
